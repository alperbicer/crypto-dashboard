import actions from './listing.actions';
import getters from './listing.getters';
import mutations from './listing.mutations';
import state from './listing.state';
export default {
  strict: true,
  actions,
  getters,
  mutations,
  state,
};
